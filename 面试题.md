## 面试问题

### 1. 移动端事件, 移动端特点

**移动端事件**

| 事件          | 描述      |
| ----------- | ------- |
| touchstart  |         |
| touchmove   |         |
| touchend    |         |
| touchcancel | 系统停止跟踪时 |

**移动端项目特点**

只需要兼容 webkit内核的浏览器, 以及 Chrome, uc, qq 等其他浏览器.

### 2. 300ms延迟

FastClick

### 3. 移动端 AJAX

通过 JQ, 常用参数(url,type,data,datatype,async,success,error)

#### 出现 ERROR 中 ?

跨域, datatype 出问题

### 4. AJAX 特点

异步, 局部刷新, 按需获取资源, 节约带宽

### 5. 响应式

媒体查询

### 6. 模板引擎 缺点

 用正则表达式, 检测标签并替换

缺点: 配合 ajax 不利于 seo.bug难找

### 7. 移动端跨域

### 8. JSONP

只支持 get 请求

### 9. BOM

location

document

history

### 10. 获取浏览器信息

### 11. VUE 核心

数据双向绑定

MVVM

自定义指令

生命周期

虚拟dom树

### 12.  JQ常用 API

选择器

CSS

ajax

html

各种过渡动画

### 13.  JQ选择器封装

根据参数开头符号来判断

### 14. LESS

CSS预处理器

可以设置变量 `@`

可以继承

### 15. CSS3

调用底层 C++ 来实现

效果更好

### 16. 闭包

优点: 避免全局污染, 保存私有变量

缺点: 更多的内存消耗

使用场景: 模块发开发, 封装

### 17. 多页面通讯

本体存储 

### 18. H5 新特性

新增的语义化标签

多媒体标签

盲人设备解析

画布

### 19. locals…….

### 20. 通过封装实现 JS 兼容

### 21. 栅格系统

分成12份

媒体查询

### 22. CSS3 新特性

### 23. 判断用户登录

### 24. zeputo 与 JQ 区别, 以及常见 API

### 25. 原型链

### 26. 继承

### 27. 原生事件委托

### 28. 事件三阶段

捕获

处理

冒泡

### 29. ES6 新特性

箭头函数

模板字符串

let const

结构性赋值

class

promise: 解决回调地狱

### 30. 性能优化

压缩图片

减少请求次数

减少 DNS 查询

使用揽加载

### 31. 重绘 重排





## JavaScript

### 常见的浏览器存储技术 - 未完成

### Ajax

#### Ajax的优缺点 - 未完成

### 正则表达式 --- JS高级-第五天

#### 案例

> 一. 问题: ^d+\[^d\] 可以匹配那个?   **C**
>
> a. 123
>
> b. 123a
>
> **c. d123**
>
> d. 123def

```javascript
var regExp = /^d+[^d]/;

console.log('A', regExp.test('123'));
console.log('B', regExp.test('123a'));
console.log('C', regExp.test('d123')); // true
console.log('D', regExp.test('123def'));

/**
 * 题目里的 'd' 代表 字符串 'd', 而不是代表 0~9 的 '\d'
 * '^' 表示开头, 第一位必须是 'd'
 * '+' 表示后面的东西可以出现一次或多次
 * '[]' 表示一个字符的范文, '[]' 里面的 '^' 代表非, 所以, 里面不能出现 'd'
 */
```

### 内置方法

#### 判断某个变量是否是为字符串对象

```javascript
let str = 'str';

let a = str instanceof String;
let b = typeof str === 'string';
// let c = str is String; 根本就不存在这个方法吧

console.log("a:", a);
console.log("b:", b); // true
// console.log("c:", c);

/**
 * instanceof 运算符用来测试一个对象在其原型链中是否存在一个构造函数的 prototype 属性.
 */
```

#### 下面那个方法不能解决回调陷阱 - 未解决

```javascript
Promise
Generator
async
Proxy
```

#### jQuery

#####可以用过什么方式给定不透明度

```javascript
fadeIn('slow/400/fast', function() {});
fadeOut('slow/400/fast', function() {});
fadeToggle('slow/400/fast', function() {});
/** 第二个参数用于设置不透明度 */
fadeTo('slow/400/fast', 0.5, function() {});
```

## CSS

### CSS3 新增属性 - 未完成

| 属性   | 参数   | 描述   |
| ---- | ---- | ---- |
|      |      |      |

### 块级元素中, 有一行文字, 能实现文字前两字留白的属性?

> 可以用 rem 和 em 属性配合 text-indent , 2rem或em正好首行缩进两个汉字字符.

- margin-left: 不可以,父容器整体偏移
- padding-left: 可以, 父容器不会动, 向内压缩内容
- text-indent: 可以, 用于首行缩进
- letter-spacing: 不可以, 这个设置的是字符间距

## HTML

### HTML5新增属性 - 未完成

| 属性   | 参数   | 描述   |
| ---- | ---- | ---- |
|      |      |      |

## 网络

### 网络协议 - 未完成

| 协议   | 描述   |
| ---- | ---- |
| MAC  |      |
| HTTP |      |
| SMTP |      |
| ARP  |      |
| RTSP |      |

### HTTP

####协议头描述 - 未完成

### COOKIE

#### 如何提高安全性 - 未完成


